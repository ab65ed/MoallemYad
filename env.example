NODE_ENV=production
PORT=5000

# Required in production
JWT_SECRET=change-me-please

# Optional: storage path (inside container path mapped via compose)
DATA_PATH=/data/data.json

# Future optional: if we add MongoDB adapter
MONGODB_URI=mongodb://user:pass@host:27017/dbname


